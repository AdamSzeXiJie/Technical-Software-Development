To make the project work, first, open up the .cpp file. Then right click on source to add existing files. Find sqlite-amalgamation-3360000
and add it into the source. Next right click on the assignment.sln and go to manage NuGet and install sqlite3_c_plus_plus. Lastly, copy the
folder called StoreData and placed in your c:drive to store the data.